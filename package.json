{
  "name": "pi-account-router",
  "version": "0.1.0",
  "description": "Pi extension package for routing across authenticated subscription accounts.",
  "type": "module",
  "files": [
    "src"
  ],
  "keywords": [
    "pi-package",
    "pi-extension",
    "account-router"
  ],
  "engines": {
    "node": ">=20.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markoonakic/pi-account-router.git"
  },
  "bugs": {
    "url": "https://github.com/markoonakic/pi-account-router/issues"
  },
  "homepage": "https://github.com/markoonakic/pi-account-router#readme",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "vitest --run",
    "typecheck": "tsc --noEmit",
    "check": "npm run test && npm run typecheck",
    "pack:dry": "npm pack --dry-run"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.74.0",
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@earendil-works/pi-tui": "^0.74.0",
    "@types/node": "^24.3.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
